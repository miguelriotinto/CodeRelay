org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.caching=true
org.gradle.parallel=true
kotlin.code.style=official
android.useAndroidX=true
android.nonTransitiveRClass=true

# NOTE: the former `android.experimental.lint.version=8.13.2` override (an
# AGP-8.7.3-era workaround — its bundled Lint 31.7.x crashed on Kotlin-2.3.21
# sources) is gone: AGP 8.13.2 ships a Kotlin-2.3-aware Lint by default. See
# gradle/libs.versions.toml [versions] agp.
